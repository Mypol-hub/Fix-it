-- 1. Drop the restrictive creation policy on the requests table
DROP POLICY IF EXISTS "Public creation"
ON public.requests;

-- 2. Create a truly public insert policy that accepts both guests and signed-in profiles
CREATE POLICY "Public creation"
ON public.requests
FOR INSERT
TO anon, authenticated
WITH CHECK (true);
