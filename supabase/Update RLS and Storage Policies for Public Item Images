DROP POLICY IF EXISTS "Allow public inserts hyphen" ON storage.objects;
DROP POLICY IF EXISTS "Allow public viewing hyphen" ON storage.objects;

-- Create clean policies that match your active lowercased bucket id
CREATE POLICY "Allow public inserts lowercase"
ON storage.objects FOR INSERT
TO anon, authenticated
WITH CHECK (bucket_id = 'item-images');

CREATE POLICY "Allow public viewing lowercase"
ON storage.objects FOR SELECT
TO anon, authenticated
USING (bucket_id = 'item-images');

-- Force the bucket to be public
UPDATE storage.buckets SET public = true WHERE id = 'item-images';

------------------------------------------------------------------------

-- 3. UNIFY ADMIN STATUS UPDATE PERMISSIONS (RLS)
------------------------------------------------------------------------

ALTER TABLE public.requests ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "Allow public updates for requests" ON public.requests;
DROP POLICY IF EXISTS "Unrestricted requests select" ON public.requests;

CREATE POLICY "Unrestricted requests select"
ON public.requests FOR SELECT USING
(true);

CREATE POLICY "Allow public updates for requests"
ON public.requests FOR UPDATE USING
(true) WITH CHECK (true);
