{
  "name": "Fix-it-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build --prefix client-app",
    "predeploy": "npm run build",
    "deploy": "PATH=/usr/local/bin:$PATH gh-pages -d client-app/dist"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "gh-pages": "^6.3.0",
    "vite": "^5.4.21"
  },
  "homepage": "https://mypol-hub.github.io/Fix-it"
}
